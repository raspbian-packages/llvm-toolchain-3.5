# That is normal. The lib is not (yet?) shipped as a new package
liblldb-3.5: package-name-doesnt-match-sonames liblldb
# For now, override this warning. We might create a -dev at some point
liblldb-3.5: non-dev-pkg-with-shlib-symlink usr/lib/*/liblldb.so.1 usr/lib/*/liblldb.so
